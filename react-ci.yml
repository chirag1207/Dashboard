name: React Dashboard CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-dockerize:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Step 3: Install dependencies
      - name: Install dependencies
        run: npm ci

      # Step 4: Build the app
      - name: Build Vite app
        run: npm run build

      # Step 5: Build Docker image
      - name: Build Docker image
        run: docker build -t chirag1207/dashboard:latest .

      # Step 6 (Optional): Push to Docker Hub
      # Uncomment if you want CI/CD deployment
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Push image to Docker Hub
      #   run: |
      #     docker tag chirag1207/dashboard:latest ${{ secrets.DOCKER_USERNAME }}/dashboard:latest
      #     docker push ${{ secrets.DOCKER_USERNAME }}/dashboard:latest
